{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [[ \"$f\" == *.swift ]] && swiftlint --fix --quiet \"$f\"; } 2>/dev/null || true",
            "statusMessage": "SwiftLint fixing..."
          }
        ]
      }
    ]
  }
}
